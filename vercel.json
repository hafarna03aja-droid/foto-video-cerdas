{{

  "buildCommand": "npm run build",  "buildCommand": "npm run build",

  "installCommand": "rm -f package-lock.json && npm install --no-optional",  "outputDirectory": "dist",

  "outputDirectory": "dist",  "devCommand": "npm run dev",

  "framework": "vite"  "installCommand": "npm install",

}  "framework": "vite",

  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
